# reveal-template
Template project for reveal.js.

## How To Use

### Install reveal-ck

```
gem install reveal-ck
```

### Create slides and start preview

```
reveal-ck generate; reveal-ck serve
```

You can preview slides on browser at `http://localhost:10000`

### Customize

You can customize slide with `config.yml` and `custom.css`.
